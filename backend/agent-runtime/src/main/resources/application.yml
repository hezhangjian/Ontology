spring:
  application:
    name: agent-runtime

server:
  port: 8002

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
