spring:
  application:
    name: ontology-core

server:
  port: 8000

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
