import { Empty } from 'antd';
import type { DashboardWidget } from '../types';
export default function DashboardCanvas({ active, onSelect, selected, widgets }: { active: string; onSelect: (id: string) => void; selected?: string; widgets: DashboardWidget[] }) { const values = widgets.filter((item) => item.pageId === active).sort((a,b) => a.layout.desktop.y-b.layout.desktop.y); return values.length ? <div className="dashboard-editor-canvas">{values.map((widget) => <button className={selected === widget.id ? 'editor-widget selected' : 'editor-widget'} key={widget.id} onClick={() => onSelect(widget.id)} style={{gridColumn:`span ${widget.layout.desktop.w}`}}><strong>{widget.title}</strong><span>{widget.type}</span><small>{widget.dataSourceId ? '已绑定数据源' : '静态组件'}</small></button>)}</div> : <Empty description="从左侧添加组件" />; }
